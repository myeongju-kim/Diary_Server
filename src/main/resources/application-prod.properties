# DB SETTING
spring.datasource.url = ${PROD_DB_URL}
spring.datasource.username = ${PROD_DB_USERNAME}
spring.datasource.password = ${PROD_DB_PASSWORD}
spring.datasource.driver-class-name = ${PROD_DB_CLASS_NAME}

# JPA Setting
sprint.jpa.database=${PROD_DB_TYPE}

# SOP RANGE
cors.range = ${PROD_CORS_RANGE}